input {
  jdbc {
    jdbc_driver_library => "/usr/share/logstash/drivers/mssql-jdbc-12.4.2.jre11.jar"
    jdbc_driver_class => "com.microsoft.sqlserver.jdbc.SQLServerDriver"
    jdbc_connection_string => "jdbc:sqlserver://localhost:1433;databaseName=elevens-bank;encrypt=true;trustServerCertificate=true;"
    jdbc_user => "sa"
    jdbc_password => "StrongP@ssw0rd!"
    schedule => "* * * * *"
    statement => "SELECT * FROM audit_logs WHERE log_id > :sql_last_value ORDER BY log_id ASC"
    use_column_value => true
    tracking_column => "log_id"
    tracking_column_type => "numeric"
    last_run_metadata_path => "/usr/share/logstash/pipeline/.audit_log_jdbc_last_run"
  }
}

filter {
  mutate {
    remove_field => ["@version", "@timestamp"]
  }
}

output {
  kafka {
    bootstrap_servers => "localhost:29093"
    topic_id => "audit-logs"
    codec => json
  }
  stdout { codec => rubydebug }
}
